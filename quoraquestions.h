#include <stdio.h>
#include "boysandgirls.c"
#include "highscore.c"
int basicio(); //if we want to type out our functions below main() we must first declare them up top.
int boysorgirls();
int highscore(int player);
